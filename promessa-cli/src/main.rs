//! `promessa` CLI — operator surface stub per VIGGY-AUTHORING §11.2.
//! Full subcommand catalog (get / history / inspect / verify / replay)
//! follows once `promessa-runtime` + `promessa-store` ship.

fn main() {
    tracing_subscriber::fmt::init();
    eprintln!("promessa CLI — Viggy M1 stub. Subcommand surface pending per VIGGY-AUTHORING §11.2.");
}
