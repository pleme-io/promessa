[package]
name = "camelot-posture-controller"
description = "The tick-by-tick camelot-posture PromessaController — a Viggy TargetController that continuously enforces the WHOLE camelot posture (breathe mem/cpu/storage bands at setpoint, scale-to-zero, 100% spot, arm, never-stuck, no-leak, no-errors, no over-provisioning) as ONE typed invariant set. Composes breathe + the autorevivy leak-guards + the seal; shadow-first, no blind remediation."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
promessa-types = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
